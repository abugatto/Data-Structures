#ifndef MSP_CPP
#define MSP_CPP

#include <iostream>
#include "Graph.h"

//////////////////////////////////////////////////////////////////////////////////
/// Implements Minimum Spanning Tree routines in modern C++11
//////////////////////////////////////////////////////////////////////////////////

template<typename NData, typename EData>
PathPtr<NData,EData> prim(const GraphPtr<NData,EData>& graph) {

}

template<typename NData, typename EData>
PathPtr<NData,EData> kruskal(const GraphPtr<NData,EData>& graph) {

}

template<typename NData, typename EData>
PathPtr<NData,EData> boruvka(const GraphPtr<NData,EData>& graph) {

}

#endif