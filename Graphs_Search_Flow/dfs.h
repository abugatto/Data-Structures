#ifndef DFS_CPP
#define DFS_CPP

//////////////////////////////////////////////////////////////////////////////////
/// Implements Depth First Search routines in modern C++11
//////////////////////////////////////////////////////////////////////////////////

#include <iostream>
#include "Graph.h"

template<typename NData, typename EData>
TreePtr<NData,EData> depthFirstSearch(const GraphPtr<NData,EData>& graph, const NodePtr<NData,EData>& src) {

}

#endif